'use strict'

module.exports = function($scope, db)
{
	console.log('LOGIN', data('login'))
	//$scope.nextItem = function(event, self)
	//{
	//	console.log(event, self, this)
	//	var img = document.createElement('img')
	//
	//	img.src = URL.createObjectURL(event.target.files[0])
	//
	//	img.height = '150'
	//
	//	var counter = document.getElementById('count')
	//
	//	var count = +counter.getAttribute('count') + 1
	//
	//	counter.setAttribute('count', count)
	//
	//	counter.innerHTML = count+' Items'
	//
	//	URL.revokeObjectURL(img.src)
	//
	//	node.parentNode.parentNode.insertBefore(img, node.parentNode.nextSibling);
	//}

}